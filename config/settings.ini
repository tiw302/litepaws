# LitePaws Configuration File

# Path to the pet image (relative to executable or absolute)
texture_path=assets/pet.png

# Initial Window Size (should match your sprite frame size roughly)
width=128
height=128

# Movement Speed (pixels per second)
speed=50
